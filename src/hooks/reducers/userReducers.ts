import { createSlice } from '@reduxjs/toolkit'

const itens ={
    produtos: []
}
const itensSlice = createSlice({    
        name: 'name',
        description: 'A',
        price: 100,
        photo: 'A',
        reducers: {

        }
    
})